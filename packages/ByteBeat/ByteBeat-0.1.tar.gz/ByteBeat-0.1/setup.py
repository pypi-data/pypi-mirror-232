from setuptools import setup

setup(name='ByteBeat',
      version='0.1',
      description='Run ByteBeat in Python 3!',
      author='Itzsten',
      packages=['ByteBeat'],
      zip_safe=False)
