[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "date-corrector"
version = "0.0.3"
authors = [
  { name="Adham Abdelmaksoud", email="adham.5050@hotmail.com" },
]
description = "A simple package for date correction"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]