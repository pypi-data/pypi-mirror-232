[project]
name = "simpleworkspace"
version = "1.1.306"
description = "Package created for https://spacerival.com, opensource and free to use and modify"
requires-python = ">=3.10"
dependencies = [ "requests",]

[build-system]
requires = [ "setuptools", "wheel",]
build-backend = "setuptools.build_meta"
