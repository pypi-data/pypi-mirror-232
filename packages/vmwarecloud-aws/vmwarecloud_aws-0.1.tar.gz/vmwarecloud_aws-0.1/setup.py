from setuptools import setup, find_packages

setup(
    name="vmwarecloud_aws",
    version="0.1",
    author="VMware, Inc.",
    description="Client bindings for VMware Cloud on AWS",
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)

