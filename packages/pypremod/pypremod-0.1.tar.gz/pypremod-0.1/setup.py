#!/usr/bin/env python

from setuptools import setup

setup(name='pypremod',
      description='Python package for Premod solver',
      url='https://www.sintef.no',
      version = '0.1',
      packages=['premod'])
