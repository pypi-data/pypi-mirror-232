[tool.poetry]
name = "zerogpu"
version = "0.1.0"
description = "Helpers for Hugging Face ZeroGPU Spaces"
authors = ["cbensimon <charles@huggingface.co>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
