[tool.poetry]
name = "shellm"
version = "0.4.0"
description = ""
authors = ["Gabriel Altay <gabriel.altay@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
shellm = "shellm.main:app"

[tool.poetry.dependencies]
python = "^3.11"
typer = {extras = ["all"], version = "^0.9.0"}
openai = "^0.28.0"
pydantic = "^2.3.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
