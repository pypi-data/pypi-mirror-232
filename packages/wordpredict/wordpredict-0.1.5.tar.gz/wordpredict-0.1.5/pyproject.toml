[tool.poetry]
name = "wordpredict"
version = "0.1.5"
description = "word predictor"
authors = ["2ndr9"]
repository = "https://github.com/2ndr9/wordpredict"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
