from setuptools import setup, find_packages

setup(
    name="arithmetic.py",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your library dependencies here
    ],
)
