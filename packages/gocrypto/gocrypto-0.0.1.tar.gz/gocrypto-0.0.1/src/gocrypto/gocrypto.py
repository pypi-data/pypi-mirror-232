
# python wrapper for package pixelview.io/gocrypto within overall package gocrypto
# This is what you import to use the package.
# File is generated by gopy. Do not edit.
# gopy build -output=out -vm=python3 pixelview.io/gocrypto

# the following is required to enable dlopen to open the _go.so file
import os,sys,inspect,collections
try:
	import collections.abc as _collections_abc
except ImportError:
	_collections_abc = collections

cwd = os.getcwd()
currentdir = os.path.dirname(os.path.abspath(inspect.getfile(inspect.currentframe())))
os.chdir(currentdir)
from . import _gocrypto
from . import go

os.chdir(cwd)

# to use this code in your end-user python file, import it as follows:
# from gocrypto import gocrypto
# and then refer to everything using gocrypto. prefix
# packages imported by this package listed below:




# ---- Types ---


#---- Enums from Go (collections of consts with same type) ---


#---- Constants from Go: Python can only ask that you please don't change these! ---


# ---- Global Variables: can only use functions to access ---


# ---- Interfaces ---


# ---- Structs ---

# Python type for struct gocrypto.Data
class Data(go.GoClass):
	""""""
	def __init__(self, *args, **kwargs):
		"""
		handle=A Go-side object is always initialized with an explicit handle=arg
		otherwise parameters can be unnamed in order of field names or named fields
		in which case a new Go object is constructed first
		"""
		if len(kwargs) == 1 and 'handle' in kwargs:
			self.handle = kwargs['handle']
			_gocrypto.IncRef(self.handle)
		elif len(args) == 1 and isinstance(args[0], go.GoClass):
			self.handle = args[0].handle
			_gocrypto.IncRef(self.handle)
		else:
			self.handle = _gocrypto.gocrypto_Data_CTor()
			_gocrypto.IncRef(self.handle)
			if  0 < len(args):
				self.String = args[0]
			if "String" in kwargs:
				self.String = kwargs["String"]
			if  1 < len(args):
				self.Key = args[1]
			if "Key" in kwargs:
				self.Key = kwargs["Key"]
	def __del__(self):
		_gocrypto.DecRef(self.handle)
	def __str__(self):
		pr = [(p, getattr(self, p)) for p in dir(self) if not p.startswith('__')]
		sv = 'gocrypto.Data{'
		first = True
		for v in pr:
			if callable(v[1]):
				continue
			if first:
				first = False
			else:
				sv += ', '
			sv += v[0] + '=' + str(v[1])
		return sv + '}'
	def __repr__(self):
		pr = [(p, getattr(self, p)) for p in dir(self) if not p.startswith('__')]
		sv = 'gocrypto.Data ( '
		for v in pr:
			if not callable(v[1]):
				sv += v[0] + '=' + str(v[1]) + ', '
		return sv + ')'
	@property
	def String(self):
		return _gocrypto.gocrypto_Data_String_Get(self.handle)
	@String.setter
	def String(self, value):
		if isinstance(value, go.GoClass):
			_gocrypto.gocrypto_Data_String_Set(self.handle, value.handle)
		else:
			_gocrypto.gocrypto_Data_String_Set(self.handle, value)
	@property
	def Key(self):
		return _gocrypto.gocrypto_Data_Key_Get(self.handle)
	@Key.setter
	def Key(self, value):
		if isinstance(value, go.GoClass):
			_gocrypto.gocrypto_Data_Key_Set(self.handle, value.handle)
		else:
			_gocrypto.gocrypto_Data_Key_Set(self.handle, value)


# ---- Slices ---


# ---- Maps ---


# ---- Constructors ---


# ---- Functions ---
def Decrypt(inputString, passphrase):
	"""Decrypt(str inputString, str passphrase) str"""
	return _gocrypto.gocrypto_Decrypt(inputString, passphrase)
def Encrypt(inputString, passphrase):
	"""Encrypt(str inputString, str passphrase) str"""
	return _gocrypto.gocrypto_Encrypt(inputString, passphrase)
def Hi(goRun=False):
	"""Hi() """
	_gocrypto.gocrypto_Hi(goRun)


