[tool.poetry]
name = "instadump"
version = "0.2.1"
description = ""
authors = ["redraw <redraw@sdf.org>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
httpx = "^0.25.0"
aiofiles = "^23.2.1"
pyyaml = "^6.0.1"
python-dateutil = "^2.8.2"
tqdm = "^4.66.1"
typer = "^0.9.0"

[tool.poetry.scripts]
instadump = "instadump.cli:app"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.2"
pytest-mock = "^3.11.1"
coverage = "^7.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
