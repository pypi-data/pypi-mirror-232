[tool.poetry]
name = "microagi-kubectl"
version = "0.0.1"
description = "AKube - #microAGI agent assistant translating natural language commands to kubectl actions"
authors = ["Sivan Grünberg <sivan@vitakka.co>"]
readme = "README.md"
packages = [
    { include = "adock" },
]

[tool.poetry.dependencies]
python = "^3.11"
openai = "^0.27.8"
requests = "^2.31.0"
pydantic = "^2.0.3"
python-dotenv = "^1.0.0"
colorama = "^0.4.6"
autopage = "^0.5.1"

[tool.poetry.scripts]
adock = "adock.main:async_main"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
