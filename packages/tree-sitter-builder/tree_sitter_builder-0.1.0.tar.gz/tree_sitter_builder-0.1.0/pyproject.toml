[tool.poetry]
name = "tree-sitter-builder"
version = "0.1.0"
description = ""
authors = ["Naoya Yamashita <conao3@gmail.com>"]
license = "Apache-2.0"
readme = "README.md"

[tool.poetry.scripts]
tree-sitter-builder = "tree_sitter_builder.__main__:main"

[tool.poetry.dependencies]
python = "^3.11"
tree-sitter = "^0.20.1"
platformdirs = "^3.9.1"
pyyaml = "^6.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
