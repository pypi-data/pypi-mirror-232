[build-system]
requires = ['setuptools']
build-backend = 'setuptools.build_meta'

[project]
name='serializetool'
version='0.01'
description = 'Yolo model serialize'
readme = {file = 'README.md', content-type='text/markdown'}
requires-python = '>=3.7'
license = {text = "MIT License"}

