version: '3'

services:
  api:
    build:
      context: .
      args:
        CHANNEL_TASKS_PACKAGE: $CHANNEL_TASKS_PACKAGE
    volumes:
      - $CHANNEL_TASKS_DOCKER_HOME:/www
    ports:
      - $CHANNEL_TASKS_PORT:8001
    depends_on:
      - db
    environment:
      - CHANNEL_TASKS_DB_PASSWORD
      - TASK_ADMIN_USER
      - TASK_ADMIN_EMAIL
      - LC_ALL=C.UTF-8
      - LANG=C.UTF-8
      - LANGUAGE=C:en
    command: sh -c "./setup-channel-tasks-docker.sh && exec /usr/local/bin/docker-entrypoint.sh unitd --no-daemon"
  db:
    image: postgres:latest
    environment:
      - PGPORT=5433
      - POSTGRES_PASSWORD=postgres
  redis:
    image: redis:latest
