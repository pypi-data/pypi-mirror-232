[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "FileScanner"
version = "1.1.2"
authors = [
  { name="Ryan Sanders", email="rasanders22@gmail.com" },
]
description = "Scans files based on input array"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]